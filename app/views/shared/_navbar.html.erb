<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #E6E6FA">
  <a class="navbar-brand" href="#">Navbar</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href=<%= admin_products_path %>>Products</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href=<%= parent_categories_admin_categories_path %>>Categories</a>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item">
          <%= button_to 'Sign out', destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% else %>
        <li class="nav-item">
          <a class="nav-link" href=<%= new_user_session_path %>>Sign in</a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
